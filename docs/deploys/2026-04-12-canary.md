# Canary Deploy — 2026-04-12

First production exercise of the rewritten `deploy.sh` and `rollback.sh`
per `docs/ops/blue-green-deploy-oom-runbook.md`.

Purpose: prove the bounded-overlap state machine runs cleanly end-to-end
under real GitHub Actions SSH conditions before landing the Phase 3
sitemap code changes.

This file intentionally contains no code and should not affect anything
at runtime. If you see this file in production, the canary worked.
